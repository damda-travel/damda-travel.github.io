# Project Maintenance Instructions

이 문서는 이 프로젝트를 이어서 수정하는 Codex·개발자를 위한 작업 기준입니다.

## 제품 원칙

- 대상 사용자는 남미 여행자다. 기본 언어는 스페인어, 보조 언어는 한국어다.
- 모바일 390px 전후 화면을 우선하고 데스크톱은 확장 레이아웃으로 다룬다.
- 현재 초록·청록 기반의 여행 가이드 콘셉트, 카드 한 줄 설명, 섹션 구조를 유지한다.
- 관광정보는 많아도 되지만 첫 화면 조작부와 카드에는 핵심만 노출한다.

## 데이터 신뢰도

- 공식 출처에서 확인하지 않은 운영시간·요금·체류시간을 공식 정보처럼 표시하지 않는다.
- 추천 체류시간은 `js/app.js`의 `getRecommendedStay()` 한 곳에서만 계산한다.
- 공식 체류시간을 확보한 항목은 `recommendedDurationSource: 'official'`을 함께 기록한다.
- `recommendedDuration`만 있는 기존 생성 데이터는 공식값으로 간주하지 않는다.
- 운영정보와 사진을 갱신한 경우 대응하는 `scripts/*_audit.json`도 확인한다.
- TourAPI 서비스 키를 HTML·JavaScript·Git 기록에 하드코딩하지 않는다.

## 코드 구조

- 편집 원본은 루트 `index.html`, `css/`, `js/`, `images/`다.
- 최종 시각 보정은 `css/polish.css`에만 추가한다. 기존 `css/style.css`의 누적 규칙을 다시 덮어쓰지 않는다.
- `dist/client/`은 배포 복사본이다. 배포 전 변경 파일을 동일 경로로 동기화한다.
- `js/app.js`의 상태, 렌더링, 모달, 플래너 책임을 유지하되 새 기능은 관련 함수 옆에 작게 추가한다.
- 전체 카탈로그를 반복해서 재조립하지 않는다. `ensureTourIndex()`의 캐시를 사용한다.
- 비동기 필터 결과는 `uiRequestSequence` 검사를 통과한 최신 요청만 렌더링한다.
- 스크롤 이벤트마다 직접 DOM을 갱신하지 않는다. 기존 단일 `requestAnimationFrame` 스케줄러에 합친다.
- 요청 없는 라이브러리 추가, 전면 리팩토링, 데이터 삭제, 폴더 이동을 하지 않는다.

## 성능 기준

- 초기 관광 카드는 6개를 유지하고 추가 목록은 더보기로 렌더링한다.
- 카드 이미지는 WebP, `loading="lazy"`, `decoding="async"`를 유지한다.
- 첫 화면 히어로 이미지만 preload한다.
- 모바일 고정 요소에 고비용 `backdrop-filter`를 다시 추가하지 않는다.
- 터치 환경에서 hover 확대를 적용하지 않는다.
- 애니메이션 추가 시 `prefers-reduced-motion` 동작을 보장한다.

## 필수 회귀 테스트

- 390×844: ES/KR, 검색, 지역, 테마, 정렬, 더보기
- 카드: 한 줄 설명, 예상 체류시간, 이미지 실패 대체 화면
- 상세: 긴 설명 펼치기·접기, 체류시간 출처 라벨, 공식 링크, Google Maps
- 저장: 추가·삭제, 마이페이지, 저장 목록으로 일정 생성
- 플래너: 다지역 선택, 일정 생성, 하단 내비게이션 활성 상태
- 새로고침: 페이지 최상단 복귀
- 1280px: 섹션 순서, 카드 그리드, 모달, 가로 넘침 없음
- 콘솔: 오류 없음
- 첫 방문 진단: ES/KR 문구, 건너뛰기, 단계 이동, 관심 테마 최대 3개, 선택 연락처 동의
- 수요 DB: 유효 입력만 `travel_demand`에 저장되고 연락처는 동의 없이는 저장되지 않음

## 배포

- 공개 프런트는 `main` 푸시 후 GitHub Actions가 `dist/client/`를 GitHub Pages에 자동 배포한다.
- `.openai/hosting.json`의 기존 Sites `project_id`는 D1 기반 여행 수요 API 서버에 계속 재사용한다.
- `dist/server/` 변경 시 Sites 서버도 함께 배포하고, GitHub Pages 출처의 CORS 사전 요청을 검증한다.
- 변경 소스를 `dist/client/`에 동기화하고 검증한 커밋만 저장·배포한다.
- 이 프로젝트는 사용자가 자동 배포를 상시 요청했으므로 검증 완료 후 푸시와 배포를 연속해서 진행한다.
- `scripts/local_server.js`는 로컬 검수용 사용자 파일이다. 명시 요청 없이 삭제하거나 커밋하지 않는다.
